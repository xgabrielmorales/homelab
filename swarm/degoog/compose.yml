# vim: ft=yaml.docker-compose

services:
  degoog:
    image: ghcr.io/degoog-org/degoog:0.20.0@sha256:77776b4258a879b8cfdc322791b476aac9839daa84dbaa4099a3be729c6ae182
    hostname: degoog
    volumes:
      - degoog-data:/app/data
    environment:
      - PUID=1000
      - PGID=1000
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
    networks:
      - reverse-proxy-swarm
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == worker
      restart_policy:
        condition: any
        delay: 30s
        window: 120s
      resources:
        limits:
          memory: 256M
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.degoog.rule=Host(`search.xgabrielmorales.com`)"
        - "traefik.http.routers.degoog.entrypoints=websecure"
        - "traefik.http.routers.degoog.tls.certresolver=cloudflare"
        - "traefik.http.services.degoog.loadbalancer.server.port=4444"

volumes:
  degoog-data:

networks:
  reverse-proxy-swarm:
    external: true
