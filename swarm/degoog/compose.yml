# vim: ft=yaml.docker-compose

services:
  degoog:
    image: ghcr.io/degoog-org/degoog:0.18.0@sha256:504778a7250e3c066ff08bd29df1b09238f23b49fa6c33c2256d1e2056747a58
    hostname: degoog
    volumes:
      - degoog-data:/app/data
    environment:
      - PUID=1000
      - PGID=1000
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
    networks:
      - reverse-proxy-swarm
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == worker
      restart_policy:
        condition: any
        delay: 30s
        window: 120s
      resources:
        limits:
          memory: 256M
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.degoog.rule=Host(`search.xgabrielmorales.com`)"
        - "traefik.http.routers.degoog.entrypoints=websecure"
        - "traefik.http.routers.degoog.tls.certresolver=cloudflare"
        - "traefik.http.services.degoog.loadbalancer.server.port=4444"

volumes:
  degoog-data:

networks:
  reverse-proxy-swarm:
    external: true
