services:
  traefik:
    image: traefik:latest
    container_name: traefik
    restart: always
    depends_on:
      tinyauth:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    environment:
      - CF_DNS_API_TOKEN=${TFK_CF_DNS_API_TOKEN}
      - TZ=${TFK_TZ}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
      - traefik-certs:/certs
    command:
      # API and Dashboard
      - "--api.dashboard=true"
      - "--api.insecure=false"
      - "--api.debug=false"
      # Entrypoints
      - "--entrypoints.web.address=:80"
      - "--entrypoints.web.http.redirections.entrypoint.to=websecure"
      - "--entrypoints.web.http.redirections.entrypoint.scheme=https"
      - "--entrypoints.web.http.redirections.entrypoint.permanent=true"
      - "--entrypoints.websecure.address=:443"
      # Providers
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      # Certificates ACME with Cloudflare
      - "--certificatesresolvers.cloudflare.acme.email=${TFK_ACME_EMAIL}"
      - "--certificatesresolvers.cloudflare.acme.storage=/certs/acme.json"
      - "--certificatesresolvers.cloudflare.acme.dnschallenge=true"
      - "--certificatesresolvers.cloudflare.acme.dnschallenge.provider=cloudflare"
      - "--certificatesresolvers.cloudflare.acme.dnschallenge.resolvers=1.1.1.1:53,1.0.0.1:53"
      # Logging (opcional)
      - "--accesslog=true"
      - "--global.sendAnonymousUsage=false"
      - "--log.level=INFO"
      - "--ping"
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      start_period: 5s
      start_interval: 3s
      interval: 10s
      timeout: 5s
      retries: 5
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.traefik.rule=Host(`traefik.xgabrielmorales.com`)"
      - "traefik.http.routers.traefik.entrypoints=websecure"
      - "traefik.http.routers.traefik.tls.certresolver=cloudflare"
      - "traefik.http.routers.traefik.service=api@internal"
      - "traefik.http.routers.traefik.middlewares=tinyauth"

  tinyauth:
    image: ghcr.io/steveiliop56/tinyauth:v3
    container_name: tinyauth
    restart: always
    environment:
      - APP_TITLE=${TA_APP_TITLE}
      - APP_URL=https://auth.xgabrielmorales.com
      - COOKIE_SECURE=${TA_COOKIE_SECURE:-true}
      - DISABLE_CONTINUE=${TA_DISABLE_CONTINUE:-true}
      - LOG_LEVEL=${TA_LOG_LEVEL:-0}
      - SECRET=${TA_SECRET}
      - USERS=${TA_USERS}
    healthcheck:
      test: ["CMD", "curl", "--fail", "--head", "http://localhost:3000/"]
      start_period: 10s
      start_interval: 3s
      interval: 10s
      timeout: 5s
      retries: 5
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tinyauth.rule=Host(`auth.xgabrielmorales.com`)"
      - "traefik.http.routers.tinyauth.entrypoints=websecure"
      - "traefik.http.routers.tinyauth.tls.certresolver=cloudflare"
      - "traefik.http.middlewares.tinyauth.forwardauth.address=http://tinyauth:3000/api/auth/traefik"

volumes:
  traefik-certs:
    name: traefik-certs

networks:
  default:
    name: reverse-proxy
    external: true
