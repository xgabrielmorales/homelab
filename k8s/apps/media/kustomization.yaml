apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - pvcs.yaml
  - qbittorrent.yaml
  - flexget.yaml
  - jellyfin.yaml
  - filebrowser.yaml
configMapGenerator:
  - name: qbittorrent-conf
    files:
      - files/qBittorrent.conf
  - name: flexget-conf
    files:
      - files/config.yml
